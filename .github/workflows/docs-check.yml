name: Docs check

on:
  pull_request:
    paths:
      - "**/*.md"
      - "**/*.html"
      - ".claude/templates/scripts/*.py"
  push:
    branches: [main]
    paths:
      - "**/*.md"
      - "**/*.html"
      - ".claude/templates/scripts/*.py"

jobs:
  check-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check relative Markdown and HTML links
        run: python3 .claude/templates/scripts/check_links.py
      - name: Check element-ID references
        run: python3 .claude/templates/scripts/check_model.py
